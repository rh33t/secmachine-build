---
- name: install python and pip
  apt:
    name:
      - python3-pip
    state: present
  become: true

# apt ships these as importable modules as well as CLIs, which a pipx venv cannot do
- name: install python security tools
  apt:
    name:
      - python3-impacket
      - python3-pwntools
    state: present
  become: true
