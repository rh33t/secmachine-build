---
- name: ensure tmux is installed
  apt:
    name: tmux
    state: present
    update_cache: true
  become: true

- name: deploy tmux configuration
  template:
    src: tmux.conf.j2
    dest: "~/.tmux.conf"
    mode: '0644'